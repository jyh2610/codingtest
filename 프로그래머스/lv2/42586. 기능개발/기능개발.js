function solution(progresses, speeds) {
const rel = []

let count =0
while(progresses.map(item => item >100){ 
   count ++
   progresses = progresses.map((item , i )=>{
        item = item + speeds[i]
    })
   
}
    return rel
}